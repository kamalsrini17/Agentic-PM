import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'NextGen Trading Bot - AI-Powered Personal Trading',
  description: 'The first AI trading bot that personalizes strategies based on your capital amount, optimizes between small-cap and mid-cap opportunities, and adapts to your preferred trading timeframe.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}